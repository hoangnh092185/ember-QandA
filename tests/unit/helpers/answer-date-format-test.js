
import { answerDateFormat } from 'qand-a/helpers/answer-date-format';
import { module, test } from 'qunit';

module('Unit | Helper | answer date format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerDateFormat([42]);
  assert.ok(result);
});

