
import { answerFormatDate } from 'qand-a/helpers/answer-format-date';
import { module, test } from 'qunit';

module('Unit | Helper | answer format date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answerFormatDate([42]);
  assert.ok(result);
});

